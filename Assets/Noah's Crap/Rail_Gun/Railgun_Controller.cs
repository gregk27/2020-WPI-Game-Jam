using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Railgun_Controller : MonoBehaviour
{
    /*
    [SerializeField]
    GameObject Railgun;
    Animator RailgunController;

    [SerializeField]
    Transform enemy;

    int rangeH;
    int rangeR;
    int ammo;
    public Vector2 aimOrigin;
    double aimDirection;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        Debug.DrawRay(transform.position, Vector2.right, Color.magenta, 0.1f);
    }
    bool canSeeEnemy(float distance)
    {
        var castDist = rangeH;
        bool val = false;

        RaycastHit2D hit = Physics2D.Raycast(transform.position, Vector2.right);
    }*/
}
